#!/bin/sh
# Full preflight scan before pushing (secrets, licenses, private-dir leakage).
node scripts/preflight.mjs || {
  echo "" >&2
  echo "✖ pre-push blocked by preflight (see above)." >&2
  exit 1
}
